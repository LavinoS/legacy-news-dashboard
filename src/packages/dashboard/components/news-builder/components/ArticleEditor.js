import React from 'react';
import { LegacyDiv } from '../../../../../components';

export default (props) => {
  return (
    <LegacyDiv
      styleProps={{
        ALL_DEVICES: { width: '500px', height: '500px', background: '#ff0' },
      }}
    >
      haideee
    </LegacyDiv>
  );
};
